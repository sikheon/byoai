# BYOAI 시스템 아키텍처 (정본 v1.1 — 2026-06-10 개정)

> **BYOAI (Bring Your Own AI)**: 사용자가 자기 AI(폰)를 들고 어느 공간에 가든,
> 그 공간의 능력을 읽어 **본인에게 최적화되게** 자동 제어하는 스마트홈 시스템.
> 공간을 옮겨다닐수록 내 AI가 똑똑해진다(복리효과).

이 문서는 그동안의 결정을 한 곳에 못박은 **정본**이다. 코드/문서가 충돌하면 이 문서가 기준.

---

## 1. 설계 원칙 (왜 이렇게 나눴나)

| 원칙 | 의미 |
|------|------|
| **연합 구조** | 폰·공간이 각자 소유물만 책임. 폰=사용자, 공간=기기·물리. 서로 읽기만. |
| **결정과 제어 분리** | LLM은 "목표값"만 정함(느려도 됨). 목표 유지는 결정론적 제어루프(빠르고 안정). |
| **환각 방지** | "방 크기" 같은 물리는 폰이 추측 X → 공간이 응답으로 자가학습한 모델을 폰이 읽기만. |
| **학습은 폰에 누적** | 수동조작=암묵 피드백 → 공간이 아니라 **휴대용 사용자 모델**에 쌓임(복리효과). |
| **점진적 안전** | 각 층은 상위가 죽어도 안전한 기본동작. 폰↓→척수가 마지막 목표 유지, 척수↓→말단 안전정지. |

**왜 LLM인가:** 임의 공간 = 기기 구성을 미리 모름 → 런타임에 처음 보는 구성 보고 즉석 추론. 하드코딩 불가.

---

## 2. 4계층 (신경계 비유)

```
┌─────────────────────────────────────────────────────────────┐
│ ① 대뇌  = 폰 (Gemma 4 E4B, llama.cpp)                         │
│    역할: 공간 descriptor + 휴대 사용자모델 → 목표값 결정       │
│    안 하는 것: 제어루프 X, 물리추측 X, 상태저장은 본인선호만   │
└───────────────┬─────────────────────────────────────────────┘
                │  TCP / 줄단위 JSON (포트 8765)  ※개발중엔 PC가 adb로 폰 대리
┌───────────────▼─────────────────────────────────────────────┐
│ ② 척수  = 라즈베리파이 (spine.py)                             │
│    역할: 능력별 제어루프 + 응답모델 자가학습 + SQLite저장      │
│           + SPACE_DESCRIPTOR 발행 + 수동조작→FEEDBACK 감지     │
│    안 하는 것: 결정 X(목표는 폰이 줌), 사용자선호 저장 X       │
└───────────────┬─────────────────────────────────────────────┘
                │  USB 시리얼 / 줄단위 JSON (115200 baud)
┌───────────────▼─────────────────────────────────────────────┐
│ ③ 말단  = Arduino (spine_node.ino)                            │
│    역할: 센서 읽기 + 액추에이터 PWM/릴레이 구동, 로직 0        │
│    안 하는 것: 판단 X, 저장 X — "근육·감각신경"일 뿐          │
└───────────────┬─────────────────────────────────────────────┘
                │  GPIO
┌───────────────▼─────────────────────────────────────────────┐
│ ④ 환경  = 미니룸(박스) + 센서/액추에이터                      │
└─────────────────────────────────────────────────────────────┘
```

**계층별 "안 하는 것"이 핵심.** 각 층이 자기 책임을 넘으면 환각·결합도·단일장애점이 생긴다.

---

## 3. 멀티모달 능력 모델 (BYOAI는 온도 전용이 아니다)

공간을 여러 감각으로 파악해야 "어느 공간 가도 최적화"가 성립. **키트 센서 = 능력(capability)**.

| 센서 | 능력 `kind` | 액추에이터 | 응답속도 | 발열체 | 단계 |
|------|------------|-----------|---------|--------|------|
| 조도 LDR | `light` | LED PWM | 즉각 | ❌ | **Phase 1 (첫 데모)** |
| 미세먼지 | `air_quality` | 팬(릴레이/MOSFET) | 빠름 | ❌ | 1b |
| PIR | `occupancy` | (신호만) | 즉각 | ❌ | 1b |
| 온습도 DHT | `climate` | 히터(릴레이) | 느림(분) | ✅ | 2 |

> **첫 물리 데모 = `light`**: 즉각수렴·안전·추가구매 0. "손으로 가리면 LED 밝아짐"이 눈에 보임.
> 능력은 `#define`/DB행 추가로 자유롭게 붙임 → **자유로운 기기 추가** 요구 충족.

**능력 객체 스키마 (이종 기기 수용):**
```json
{
  "kind": "light",
  "current": 120, "min": 0, "max": 1000, "unit": "lux",
  "actuator": {"type": "pwm", "pin": "D9", "range": [0, 255]},
  "model": { /* 학습된 응답모델, 능력마다 형태 다름 → JSON */ }
}
```
`kind`별로 `model` 형태가 다르므로(조도=선형, 난방=RC) **고정 컬럼 RDB는 부적합** → JSON 수용.

---

## 4. 통신 프로토콜

### 4.1 시리얼 (말단 ↔ 척수) — 115200, 줄단위 JSON
- **Arduino → RPi (텔레메트리, 1초)**: 연결된 센서만
  `{"t":21.3,"h":45.0,"lux":120,"pm":35,"pir":1}`
- **RPi → Arduino (명령)**: 연결된 액추에이터만
  `{"heater":0,"light":180,"fan":1}`  (light=0~255 PWM, heater/fan=0/1)

### 4.2 TCP (척수 ↔ 대뇌) — 줄단위 JSON, 포트 8765 (앱때 WebSocket 승격)
| 메시지 | 방향 | 내용 |
|--------|------|------|
| `SPACE_DESCRIPTOR` | RPi→폰 | 장소 + **능력 배열**(현재값·범위·학습모델) + occupancy |
| `COMMAND` | 폰→RPi | `{"targets":{"light":350,"climate":23.5},"reason":"..","preheat_minutes":15}` |
| `STATE` | RPi→폰 | (주기) 능력별 현재값/목표/액추에이터 상태 |
| `FEEDBACK` | RPi→폰 | (수동보정 감지) `{"capability":"light","from":300,"to":450,"context_label":".."}` |

### 4.3 자동발견 (Zero-config)
- mDNS `_byoai._tcp` (폰=NsdManager, RPi=zeroconf) → **기기 자동추가 UX**(Chromecast처럼).

---

## 5. 저장 (어디에 무엇을)

### 5.1 척수(RPi) = SQLite + JSON 하이브리드
이종 능력은 JSON, 균일 텔레메트리는 행으로.
```sql
devices(id, kind, capability_json TEXT, added_at)        -- 능력=JSON, 자유추가
telemetry(device_id, ts, metric, value)                  -- 시계열, 균일
learned_models(device_id, model_json TEXT, updated_at)   -- 응답모델=JSON
```
- 온보딩: 말단이 announce → `devices`에 INSERT → 이후 descriptor는 **DB에서 생성**.
- `sqlite3`는 파이썬 stdlib → 설치 0, PC부하 0.

### 5.2 대뇌(폰) = 휴대용 사용자 모델 (JSON 파일)
```json
{
  "climateOffset": +2.5, "lightPref": 380, "airMax": 40,
  "corrections": [{"context": {"time":"저녁","outdoor":12}, "kind":"climate", "delta":+2.5}]
}
```
- **공간이 아니라 폰에 저장** → 공간 옮겨다녀도 따라옴 = 복리효과의 물리적 근거.
- 저장 단위는 (맥락피처 + 보정값) 쌍 — "24도 좋아함"이 아니라 "이런 맥락엔 24도".

### 5.3 Matter 표준은?
- **방향은 맞음**: Endpoint/Cluster/Attribute = 능력모델, commissioning = 자동추가.
- **부족**: 열모델/휴대 선호 이식은 Matter에 없음. 풀 Matter는 텀프로젝트엔 과함.
- **결정**: Matter **개념만 차용**(가벼운 자체 JSON 능력모델), `kind`→Matter cluster 매핑 여지만 남겨 미래 브릿지 가능하게.

---

## 6. 학습 루프 (복리효과)

```
수동조작 감지(척수) → FEEDBACK → 폰이 맥락과 함께 휴대모델에 누적
   ↓
다음 공간 진입 → Gemma가 "이 사용자는 평소보다 +2.5도" 근거로 결정
   ↓
공간이 달라도 학습한 선호가 반영됨  ← 2026-06-01 시뮬로 실증 완료
```
신호 해석: 직접 조작=정답 라벨 / 안 건드림=약한 긍정 / 끔=강한 부정.
LLM은 보정의 원인 해석(일시적 vs 영구적)에, 저장소는 구조화 메모리.

---

## 7. 미해결 핵심 난제 (텀프로젝트 범위로 인지·스코핑)

| # | 난제 | 텀프로젝트 처리 |
|---|------|----------------|
| ① | 능력발견 표준(열모델+선호 이식 형식) | 자체 JSON 정의로 충분, Matter 매핑 여지만 |
| ② | 신뢰/권한 scoping(남의 폰이 공간 제어) | 데모는 단일 신뢰사용자. 게스트 권한은 future work로 명시 |
| ③ | 다중 사용자 선호 충돌 | 데모는 단일 점유자(PIR=1명). 충돌해소는 future work |

> 텀프로젝트는 **데모 동작 우선**. ②③은 "안다, 그러나 범위 밖"으로 발표에 명시.

---

## 8. 빌드 단계

| 단계 | 부품 | 검증 | 상태 |
|------|------|------|------|
| **0** | RPi/PC만 | `spine.py --sim` + 폰 Gemma → 멀티능력 결정·수렴·학습 | ✅ 통과 |
| **1** | +Arduino +LDR +LED | 조도 폐루프 실물(가리면 LED↑) | 다음 |
| 1b | +미세먼지/PIR +팬 | 공기질·점유 능력 추가 | |
| 2 | +PTC히터 +단열박스 | 난방 = 진짜 RC 열역학 | 부품구매 후 |
| 3 | 박스 2개 | **복리효과 물리 실증**(학습선호 타공간 이식) | |

폰 앱(Kotlin)은 Phase 1과 병렬: ①mDNS탐색 ②기기카드 자동생성 ③AI최적화 ④수동슬라이더→학습.

---

## 9. 산출물 맵

```
gemma-test/
├─ ARCHITECTURE.md          ← 이 정본 문서
├─ byoai-sim/               Phase 0 시뮬레이터 (검증완료)
│   ├─ byoai_sim.py         Room/SpaceHub/UserModel + 폰 adb 브릿지
│   └─ README.md
├─ byoai-hw/                실물 하드웨어
│   ├─ README.md            시스템구성·배선·단계
│   ├─ arduino/spine_node/spine_node.ino   말단 펌웨어(#define 토글)
│   └─ rpi/
│       ├─ spine.py         척수(제어루프+학습+TCP+mDNS)
│       ├─ phone_brain.py   대뇌 브릿지(adb→Gemma)
│       └─ discover.py      mDNS 발견 클라이언트
└─ byoai-app/               폰 앱(Kotlin/Compose, 스켈레톤)
    └─ app/src/main/.../    discovery·net·llm·model·data·ui + cpp(JNI)
```

## 10. 확정 기술 스택
- **폰 LLM**: Gemma 4 E4B Q4_K_M, llama.cpp(b9442 검증), 4스레드 ~5.5tok/s.
- **앱**: Kotlin + Jetpack Compose, llama.cpp JNI, NsdManager(mDNS), 파일 JSON 영속.
- **척수**: Python 3, pyserial, zeroconf, sqlite3(stdlib).
- **말단**: Arduino C++, ArduinoJson.
- **프로토콜**: 시리얼 줄JSON(115200) + TCP 줄JSON(8765, →WebSocket).

---

## 11. v1.1 개정 (2026-06-10 — 구현 확정 사항)

| # | 개정 | 내용 |
|---|------|------|
| 1 | **공간 정체성 = `space_config.json`** | `SPACE_ID` 하드코딩 폐기. 척수가 첫 실행 시 MAC 기반 `space_id` 자동생성·파일 영속. 박스마다 `--config` 분리 → 박스 2개(D-2) 충돌 없음. DB도 config별(`<config>.db`). |
| 2 | **§5.1 SQLite 구현됨** | `devices`/`telemetry`/`learned_models` 가동. 텔레메트리 키→능력 **자동 온보딩**(처음 보는 센서 = announce→INSERT→연결된 폰에 즉시 반영). 재기동 시 DB에서 능력·학습모델 복원. |
| 3 | **HybridBackend (D-6)** | `--port` 기본 동작 = 실센서(lux·pir)는 시리얼, 미보유 센서(pm·t)는 sim 물리로 채움. 명령은 양쪽 모두 → LED(팬/히터 대역)는 실제 점등, sim 물리는 반응. 순수 시리얼은 `--no-fill`. |
| 4 | **COMMAND target=null = 능력 정지** | 앱 공기청정 토글 OFF가 `{"air_quality":null}` 전송 → 팬 정지. 프로토콜 의미 확장. |
| 5 | **표시 단위는 앱 책임** | 프로토콜은 물리 단위(lux·°C·µg/m³) 유지, 앱이 사용자 단위로 변환: 조명=%(범위 정규화), 공기질=등급(Good≤30/Fair≤80/Bad≤150/Danger)+on/off. |
| 6 | **물리 설계 정본 분리** | 배선·핀맵·BOM·박스 구성은 [`byoai-hw/HARDWARE.md`](byoai-hw/HARDWARE.md). LED 액추에이터 대체 매핑 포함. |
| 7 | **공간 특성 프로파일 (F-S11)** | 척수가 텔레메트리를 시간대별로 집계해 "이 공간의 이 시간대 평소값"을 자가학습. 능력 블록에 `typical_now`, descriptor에 `profile`(metric→시간대→평균, 최근 7일) 발행. 폰 프롬프트에 "현재 X (이 시간대 평소 Y)"로 주입 → 공간측 결정 근거. 환각방지 원칙의 확장: 공간 성격도 공간이 알고, 폰은 읽기만. |
```
