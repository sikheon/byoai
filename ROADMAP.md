# BYOAI 빌드 로드맵 (실행 정본 — 2026-06-10)

> 이 문서는 **무엇을 어떤 순서로 만드는가**(실행 계획)다.
> 시스템이 **어떻게 동작하는가**(설계 정본)는 [`ARCHITECTURE.md`](./ARCHITECTURE.md).
> 둘이 충돌하면: 동작 규칙은 ARCHITECTURE, 작업 순서·상태는 이 문서가 기준.

## 역할 분담 (협업 프로토콜)
- **Opus (설계·조율)**: 이 로드맵 유지, 작업 단위 정의, 완료물 리뷰/검증.
- **Fable (구현)**: 아래 작업 단위(WU)를 받아 코드 작성. 단위마다 **수용 기준**이 박혀 있어 자기검증 가능.
- 한 작업 끝나면 → 상태표 갱신 → 다음 단위. 작성과 리뷰는 분리(같은 패스에서 자기승인 금지).
- 문서 위치: 스펙=`ARCHITECTURE.md`, 계획=`ROADMAP.md`(이 파일), 모듈별 상세=각 디렉터리 `README.md`.

---

## 현재 스냅샷 (2026-06-10 저녁 — 1차 구현 스프린트 후)

| # | 모듈 | 산출물 | 상태 |
|---|------|--------|------|
| M0 | 시스템 아키텍처 정본 | `ARCHITECTURE.md` **v1.1** | ✅ 완료 |
| M0 | Phase 0 시뮬 (복리효과 실증) | `byoai-sim/` | ✅ 완료 |
| M0 | 하드웨어 설계도 | `byoai-hw/HARDWARE.md` | ✅ 완료 (회로·핀맵·BOM·박스2) |
| M1 | 척수 v2 (SQLite·공간config·Hybrid·**시간대 프로파일 F-S11**) | `byoai-hw/rpi/spine.py` | ✅ PC+Pi 스모크 통과·Pi 배포됨 |
| M1 | 말단 펌웨어 | `byoai-hw/arduino/spine_node.ino` | 🟡 핀맵 정합 완료 (실물 폐루프 미검증=WU-4.1) |
| M2 | 앱 디자인 시스템 | `byoai-app/preview.html` | ✅ 확정 |
| M2 | Compose UI (확정 디자인) | `Theme.kt` / `Screens.kt` | ✅ 재동기화 + **빌드 통과** |
| M2 | 앱 APK | `app/build/outputs/apk/debug/app-debug.apk` | ✅ `assembleDebug` 성공 (heuristic 폴백 동작) |
| M3 | 온디바이스 LLM (JNI) | `cpp/llama_jni.cpp` | 🔴 미빌드 (llama.cpp 클론 시 자동 결선되게 gradle 게이트) |
| M4 | 하드웨어 Phase 1 (조도 폐루프) | — | ⬜ 다음 (배선은 HARDWARE.md대로) |
| M5 | 통합 테스트 (Pi↔폰 end-to-end) | — | 🟡 Pi 가동 확인됨 — WU-2.3부터 가능 |

범례: ✅완료 🟡부분 🔴구버전·재작업 ⬜미착수 ⏸차단

---

## 마일스톤 & 작업 단위 (WU)

각 WU는 **Fable에게 그대로 넘길 수 있는 자족 단위**. `의존`이 끝나야 시작 가능.

### M2 — 폰 앱 완성 (지금 우선순위 1)
앱이 가장 보여주기 좋고, 디자인이 확정돼 바로 진행 가능.

- **WU-2.1 · Compose 디자인 재동기화** `의존: 없음` `✅ 완료 (2026-06-10, 실기기 스크린샷 검증)`
  - `preview.html`의 확정 디자인을 `Theme.kt`/`Screens.kt`로 이식.
  - 적용: 단일 슬레이트블루 액센트 `#86A4C8`, 다크 그래파이트 베이스, 영문 라벨(Spaces/Light/Heat/Air), 프로스티드 글래스 카드, AI Auto 칩(모드토글 아님), 조명=%·난방=°C·공기질=등급+on/off 토글.
  - 제거: 네온 4색 팔레트(`accentFor`의 무지개), AI 자동/수동 모드토글, 짜잘한 설명문구·범례·단위병기.
  - 수용 기준: 두 화면(Spaces 발견 / 공간 상세)이 `preview.html`과 시각적으로 일치. 색=단일 액센트. 한글은 콘텐츠(방 이름)에만.

- **WU-2.2 · 빌드 통과** `의존: 2.1` `✅ 완료 (assembleDebug 성공·APK 실기기 설치)`
  - Gradle 빌드를 실기기 없이 컴파일까지(`assembleDebug`) 통과. LLM 없으면 heuristic 폴백 경로로 UI 동작.
  - 수용 기준: `gradlew assembleDebug` 성공(빌드는 PC부하 고려해 `run_in_background`).

- **WU-2.3 · 프로토콜 결선 검증** `의존: 2.2` `✅ 완료 (실망 검증: mDNS 발견→연결→기기카드→AI Auto→COMMAND→STATE 라이브 반영)`
  - `SpineClient`(TCP 줄JSON) ↔ `ByoaiViewModel` ↔ UI 흐름이 `spine.py --sim`의 SPACE_DESCRIPTOR/STATE/FEEDBACK과 맞물리는지 모의 검증.
  - 수용 기준: 가짜 descriptor 주입 시 기기카드 자동생성·수동조작→FEEDBACK 발행.

### M1 — 척수 보강
- **WU-1.1 · SQLite 저장계층** `의존: 없음` `✅ 완료 (+공간config·Hybrid백엔드·자동온보딩·시간대 프로파일 F-S11, PC/Pi 스모크 통과)`
  - ARCHITECTURE §5.1 스키마(`devices`/`telemetry`/`learned_models`) 구현. descriptor를 DB에서 생성. stdlib `sqlite3`만.
  - 수용 기준: 말단 announce→INSERT→재기동 후 descriptor 복원.

- **WU-1.2 · 실시리얼 백엔드 검증** `의존: M4 하드웨어`
  - `SerialBackend`를 실제 Arduino와 115200 줄JSON으로 왕복.

### M3 — 온디바이스 LLM ※D-4 (둘 다 준비)
- **WU-3.1 · llama.cpp JNI 빌드** `의존: 2.2` `✅ 완료 (2026-06-11 실기기 검증)`
  - llama.cpp b9442 클론·arm64 Release 빌드, Gemma 챗 템플릿 수동 적용, GGUF 자동 감지 로드.
  - 검증: 폰 단독으로 descriptor+휴대모델→Gemma 결정 {light:196, air:35, climate:22} + 생성 이유 문장 UI 표시. PC/adb/클라우드 의존 0.
- **WU-3.2 · adb 브릿지 폴백 데모 경로 정비** `의존: 없음(검증됨)` ※D-4
  - JNI 실패/시간부족 대비, PC adb→폰 Gemma 호출 경로(`phone_brain.py`)를 발표용으로 정리. JNI 결과와 동일 프로토콜.
  - 수용 기준: JNI 미완 상태로도 발표 데모가 끊김없이 성립.

### M4 — 하드웨어 Phase 1 (능력 3개 + 박스 2개) ※D-2·D-3
- **WU-4.1 · 조도 폐루프** `✅ 완료 (2026-06-11)` — 조도모듈(A0)+HW-479 RGB(D9). PC 검증: light:220 명령→lux 10→35 상승(센서가 LED 빛을 봄=커플링 성립). Pi 실배선 가동.
- **WU-4.2 · 공기질·점유·외기** `✅ 완료 (2026-06-11) — 계획 초과 달성` ※D-3
  - **실센서 승격**: GP2Y10 먼지(A1+D8 실물!) + 팬 실물(릴레이→소형 5V팬) + PIR(D3) + **AM2320 외기(t_out, I2C)** — air_quality가 LED 대체 아닌 완전 실물.
  - 우노 1대 사망→교체로 해결. 펌웨어에 I2C 워치독(센서 고장 격리).
- **WU-4.3 · 공간 2개 구성 = 실물 박스 1 + 가상 공간 1** `의존: 4.2` ※D-2 수정(2026-06-11)
  - 공간A = 실물 박스(아두이노+LDR+LED+팬). 공간B = **spine --sim 인스턴스**(`--config space_b.json --tcp 8766 --mdns`).
  - 폰은 둘을 동일하게 발견·제어 → 복리효과 이식 데모 성립 + "실물/가상 동일 프로토콜" 증명.
  - 수용 기준: 폰 앱에 공간 2개 동시 표시, A 학습 → B 결정 반영.

### M5 — 통합 + 복리효과 데모 (성공 장면) ※D-2
- **WU-5.1 · 단일 공간 end-to-end** `✅ 완료 (2026-06-11 실물)` — 실박스(실센서 5종+실액추에이터) ↔ 척수(by-id 시리얼) ↔ 폰 온디바이스 Gemma 결정("사용자 선호와 공간의 평소 상태를 종합하여…" — 시간대 프로파일 인용!) → 물리 적용. +**디지털 트윈**(twin.py: 웹 관찰+가상룸 조작=선호생성+결정피드+공간기억). 공간 2개(실물A+가상B) 동시 발견.
  - SSH터널(`ssh -L 8765:127.0.0.1:8765 jongseo-pi`) → spine(실HW 또는 --sim) → 폰앱 결정·제어·학습 전 경로.
- **WU-5.2 · 복리효과 이식 실증(박스 2개)** `의존: 5.1, 4.3` `⏸`
  - 공간A: AI 최적화→사용자 수동보정→휴대모델 학습. 공간B 진입: 학습한 오프셋이 B 결정에 반영됨을 **물리로** 시연(시뮬은 검증 완료, 이번엔 실물).
  - 수용 기준: 공간A에서 보정한 선호가 공간B 목표값에 관측 가능하게 반영. = **데모의 성공 장면**.

### M6 — 미래 (스코프 밖, 명시만)
- 사용자 상태 맥락(활동량·심박·수면)을 context 피처로. (탐험 아님 — 발현선호를 더 정밀한 맥락에 매핑)
- 신뢰/권한 scoping(게스트), 다중 사용자 선호 충돌. (발표에 "안다, 범위 밖"으로 명시)

---

## 권장 진행 순서 (D-1~D-5 반영)
1. **WU-2.1** (Compose 재동기화) — 디자인 확정·의존 없음, 즉시 착수. → Fable.
2. **WU-2.2 → 2.3** (빌드·결선) — 앱을 "돌아가는 상태"로.
3. **WU-1.1** (SQLite) — 척수 영속화, 앱과 병렬 가능. → Fable.
4. **WU-4.1 → 4.2** (조도→공기질·점유) — 능력 3개 실물. → 본인 + Fable(펌웨어).
5. **WU-3.2** (adb 폴백 정비) — 발표 안전망 먼저 확보(검증된 경로).
6. **WU-3.1** (JNI) — 여유 시 실기기 LLM. 실패해도 5가 백업.
7. **WU-4.3 → WU-5.1 → 5.2** (박스 2개 → 통합 → **복리효과 실증**) — Pi 전원 확보 후, 성공 장면.

> 데모 안전망 원칙(D-1·D-4): 발표 필수 경로는 항상 **검증된 폴백**(--sim·adb·heuristic)을 갖춘다. 화려한 경로(실HW·JNI)는 그 위에 얹는다.
